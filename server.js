const http=require("http"),fs=require("fs"),path=require("path");const port=process.env.PORT||3000;http.createServer((req,res)=>{let p=req.url.split("?")[0];if(p==="/")p="/index.html";const f=path.join(__dirname,"public",p);fs.readFile(f,(e,d)=>{if(e){res.writeHead(404);return res.end("Not found")}const ext=path.extname(f);res.writeHead(200,{"Content-Type":ext===".html"?"text/html; charset=utf-8":ext===".css"?"text/css":"application/javascript"});res.end(d)})}).listen(port,()=>console.log("CASECOMMON on",port));